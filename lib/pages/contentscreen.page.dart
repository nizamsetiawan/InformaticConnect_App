import 'package:flutter/material.dart';

class ContentScreenPage extends StatelessWidget {
  const ContentScreenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
