import 'package:flutter/material.dart';

class fiturBPage extends StatelessWidget {
  const fiturBPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(16.0),
      child: Text('Ini adalah Fitur B'),
    );
  }
}